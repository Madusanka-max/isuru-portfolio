const nextConfig = {
    experimental: {
      // Remove or correct the 'appDir' key if it's not valid
      // appDir: true, // This key is not recognized and should be removed or corrected
    },
    // Other configuration options
  };
  
  export default nextConfig;